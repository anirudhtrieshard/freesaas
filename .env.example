# Server Configuration
SERVER_NAME=myserver
SERVER_IP=192.168.1.100
DOMAIN_NAME=example.com

# Kubernetes Namespaces
NAMESPACE_MATTERMOST=mattermost
NAMESPACE_MONITORING=monitoring
NAMESPACE_MEDIA=media
NAMESPACE_LANDING=landing

# Service Ports (NodePorts)
PORT_MATTERMOST=30000
PORT_PROMETHEUS=30001
PORT_GRAFANA=30002
PORT_JELLYFIN=30003
PORT_LANDING=30004

# PostgreSQL Configuration
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=mattermost
POSTGRES_USER=mmuser
POSTGRES_PASSWORD=changeme123

# Mattermost Configuration
MATTERMOST_SITE_URL=http://localhost:8065

# Grafana Configuration
GRAFANA_ADMIN_PASSWORD=changeme456

# Prometheus Configuration
PROMETHEUS_CONFIG_NAME=prometheus-config

# Cloudflare Tunnel (configured separately in ~/.cloudflared/)
# CLOUDFLARE_TUNNEL_NAME=<your-tunnel-name>
# CLOUDFLARE_TUNNEL_ID=<your-tunnel-id>
